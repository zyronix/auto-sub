#import autosub

#include $os.path.join($autosub.PATH, "interface/templates/header.tmpl")

#include $os.path.join($autosub.PATH, "interface/templates/menu.tmpl")

<form method='get' action=/config/skipShow>
Please enter which season to skip for: $title<br>
<input type='text' name='season' size='5'> <input type='submit' value='ok'><br>
Note: enter 0 to skip complete shop, enter 00 to skip special show.
<input type='hidden' name='title' value='$title'>
</form>

#include $os.path.join($autosub.PATH, "interface/templates/footer.tmpl")